# ============================================================
# Life Dashboard - 环境变量示例
# ============================================================
# 复制本文件为 .env.local 并填入真实值
#
# NEXT_PUBLIC_ADMIN_PASSWORD
#   前端构建时注入的密钥，用于请求 /api/save 和 /api/load 时做简单鉴权
#   注意：带 NEXT_PUBLIC_ 前缀的变量会在构建时被内联到前端代码中
#   本地开发: 在 .env.local 中配置后 npm run build 即可
#   Cloudflare Pages: 在项目设置 > Environment variables 中配置
#                     (Production 和 Preview 环境都需要配置)
#
# ADMIN_PASSWORD (仅后端运行时，不在前端代码中)
#   Cloudflare Pages Functions 运行时通过 env.ADMIN_PASSWORD 读取
#   仅在 Cloudflare Pages 项目设置中配置，不需要写在这里
#
# 命名约定
#   PORTFOLIO_KV  : Cloudflare KV namespace 绑定，用于保存模块数据
#   PORTFOLIO_R2  : Cloudflare R2 bucket 绑定（可选），用于外观图片等静态资源
# ============================================================

NEXT_PUBLIC_ADMIN_PASSWORD=your-secret-key-here
